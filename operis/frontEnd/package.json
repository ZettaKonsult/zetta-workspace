{
  "name": "operis",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "dependencies": {
    "react": "^16.1.0",
    "react-dom": "^16.1.0",
    "react-redux": "^5.0.6",
    "react-router-dom": "^4.2.2",
    "redux": "^3.7.2",
    "redux-form": "^7.1.2",
    "styled-components": "^2.2.3",
    "swedish-ssn": "^1.0.3",
    "uuid": "^3.1.0",
    "validator": "^9.1.2"
  },
  "devDependencies": {
    "prettier": "^1.8.2",
    "react-scripts": "1.0.17",
    "redux-devtools-extension": "^2.13.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "build": "react-scripts build",
    "upload": "aws s3 sync build/ s3://operis-app-client",
    "clean": "rm -rf ./build",
    "deploy": "yarn build && yarn run upload && yarn clean"
  }
}
