export const email = email => email.length > 0

export const password = password => password.length > 0

export const confirmPassword = (p1, p2) => password === confirmPassword

export const confirmationCode = confirmationCode => confirmationCode.length > 0
